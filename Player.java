
/**
 * Write a description of Player here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Player {
    public PlayerType playerType;
    public Type type;
    public Player(PlayerType playerType, Type type)
    {
        this.playerType = playerType;
        this.type = type;
    }
}
